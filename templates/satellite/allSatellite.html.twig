{% extends 'base.html.twig' %}

{% block title %}Satellites{% endblock %}

{% block body %}

<section class="universe">
    <div class='container'>
        <br><br>
        <h1 class="text-center text-white">Les principaux satellites du système solaire</h1>

        <div class="row mt-3">
            {% for satellite in satellite %}
                <div class="card col-md-5 mx-3 mt-5 p-3 card-satellite" style="width:30rem">
                    <img class="card-img-top" style="height:350px" src="{{satellite.photo}}">

                    <div class="card-body">
                        <h5 class="text-center card-title"><strong>{{satellite.nom}}</strong></h5>

                        <a class="btn btn-primary voir" href="{{path('app_single2_dangan', {id:satellite.id})}}">En savoir plus</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
    <br>
</section>
{% endblock %}